# Without this, git's core.safecrlf guard rejects `git add` for any file that is
# LF in the working tree on a machine with core.autocrlf=true, because the
# LF -> repo -> CRLF round trip is not reversible. Several files here are written
# with LF by tooling regardless of platform, so pinning their end-of-line form
# is more honest than converting them by hand after every regeneration.

# npm rewrites these with LF on every install / `npm pkg set`, on every platform.
package.json text eol=lf
package-lock.json text eol=lf

# Read by version managers and by CI setup-node actions, which expect LF.
.nvmrc text eol=lf
.npmrc text eol=lf

# Generated by scripts/generate-notices.js, which writes LF on every platform.
# Without this pin, core.autocrlf=true hands a fresh checkout a CRLF copy while
# the generator keeps producing LF, so test-packaging.js's byte-for-byte
# staleness comparison fails on a clean clone and `npm run notices` cannot fix
# it - regenerating writes LF, git converts it straight back to CRLF, forever.
THIRD-PARTY-NOTICES.md text eol=lf

# Generated by bench/write-manifest.js (JSON.stringify + "\n", LF on every
# platform). It pins the benchmark corpus, so a checkout that rewrote it to CRLF
# would show it as permanently modified and `node bench/write-manifest.js` could
# never clean that up - it would write LF and git would convert it straight back.
bench/manifest.json text eol=lf

# Generated by scripts/generate-logo.js, which likewise writes LF everywhere.
# This one announced itself the hard way: `git add -A` ABORTED on safecrlf, and
# because the add aborted, the commit that followed captured only the files that
# happened to be staged already - a silently partial commit. The pin is the same
# fix as the notices above; the lesson is that an aborted `add` does not abort
# the `commit` after it.
assets/logo.svg text eol=lf

# Third-party licence texts, copied VERBATIM from their upstream sources
# (assets/fonts/LICENSE-FiraCode.txt is the SIL OFL as published; the Tabulator
# licence comes out of `npm pack tabulator-tables`, at the version the vendored
# bundle's own banner declares). Their provenance is established by byte-for-byte
# comparison against the upstream artifact, so an end-of-line rewrite on checkout
# would break that verification and, for the OFL, alter the file that clause 2
# requires to travel with the font binaries. Reproducing someone else's licence
# is the one place where "git will normalise it" is the wrong answer.
#
# NO TABULATOR VERSION IS NAMED HERE, DELIBERATELY. This comment used to pin one
# - the then-current 6.2.5 - and it went stale when the bundle was bumped, which
# is worse than it sounds: 6.2.5's LICENSE reads `Copyright (c) 2015-2024` and
# 6.5.2's reads `2015-2026`, so the comment was citing an artifact whose
# copyright notice is NOT the one that ships - and under MIT the notice is the
# whole of the obligation. A version written down beside a file drifts from it;
# the version the artifact reports about itself cannot. Same reasoning, and the
# same wording, as scripts/generate-notices.js. test/test-packaging.js pins both
# halves: that no version is named here, and that the vendored LICENSE and the
# vendored bundle still come from one tarball. (That first check reads this
# whole file, so it is deliberately not satisfied by moving a pinned version
# from the citation into a comment about the citation - which is exactly what
# an earlier draft of this paragraph did, and it failed loudly.)
assets/fonts/LICENSE-FiraCode.txt text eol=lf
libs/tabulator/LICENSE text eol=lf

# Shell scripts must be LF or they fail on Linux and macOS with an unhelpful
# "bad interpreter" error when the shebang line ends in a stray carriage return.
*.sh text eol=lf

# Written by scripts/capture-theme-golden.js (JSON.stringify + "\n", LF on every
# platform), and read back byte-for-byte by test:theme. Same trap as
# bench/manifest.json above: without the pin, a CRLF checkout shows the golden
# as permanently modified and re-capturing cannot clean it up, because the
# capture writes LF and git converts it straight back.
test/fixtures/theme-golden.json text eol=lf

# The census fixture is the INPUT the golden was captured from, so both sides of
# that comparison want to be byte-stable across checkouts. Measured, not
# assumed: converting this file from CRLF to LF and re-running test:theme
# reproduces all 74 token records in both modes (76/76), so marked and Prism
# normalise line endings and the golden is not EOL-sensitive. It is pinned for
# determinism rather than correctness - and so that the "any edit here requires
# a full golden re-capture" rule is never triggered by a checkout.
test/fixtures/syntax-census.md text eol=lf

# Binary assets: never apply end-of-line conversion.
*.png binary
*.ico binary
*.icns binary
*.ttf binary
*.woff binary
*.woff2 binary
